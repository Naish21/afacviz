/** @type {import('next-sitemap').IConfig} */

module.exports = {
    siteUrl: process.env.SITE_URL || 'https://afacviz.com',
    generateRobotsTxt: true, // (optional)
     // Default transformation function
  }